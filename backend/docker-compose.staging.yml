version: '3.8'
services:
  backend:
    build: .
    image: bak55/backend:staging
    environment:
      - NODE_ENV=production
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_KEY=${SUPABASE_SERVICE_KEY}
    ports:
      - '4000:4000'
